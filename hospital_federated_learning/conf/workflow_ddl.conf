{
    "workflow": "ddl",
    "comment": "use the same dataset templates as in etl section",
    "type": "sql",

    "scripts":
    [
        {"script": "./sql_ddl/ddl_voc.sql",          "comment": ""},
        {"script": "./sql_ddl/ddl_cdm.sql",          "comment": ""}
    ]
}